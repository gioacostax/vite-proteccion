/**
 * Project project-name
 */

import { EXAMPLE } from './example';

describe('services', () => {
  test('EXAMPLE', () => {
    /* Assertions */
    expect(EXAMPLE.queryKey).toEqual(['example']);
  });
});
